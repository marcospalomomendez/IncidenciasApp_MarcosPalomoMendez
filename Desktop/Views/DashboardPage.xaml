<Page x:Class="Desktop.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      Title="Dashboard" Background="#F0F2F5">
    <Grid Background="#F0F2F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="26,20,26,12">
            <StackPanel>
                <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold" Foreground="#161D35"/>
                <TextBlock x:Name="TxtFecha" Foreground="#9AA3B0" FontSize="13" Margin="0,4,0,0"/>
            </StackPanel>
            <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                    Style="{StaticResource SecondaryBtn}" Padding="12,7"
                    Content="Actualizar" FontSize="13"
                    Click="BtnRefresh_Click"/>
        </Grid>

        <!-- WebView2 embeds Chart.js dashboard -->
        <wv2:WebView2 x:Name="WvDashboard" Grid.Row="1"/>
    </Grid>
</Page>